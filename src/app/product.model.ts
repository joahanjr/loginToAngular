export interface Product{
  // esta es la carpeta que acabamos de crear para definir los tipos de datos que tendrán el objeto
  name: string;
  price: number;
  image: string;
  category?: string;
}
