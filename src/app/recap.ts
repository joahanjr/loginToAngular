const username: string = "joahan";

const suma = (a: number, b:number) => {
  return a + b;
}

suma(2,4);

class Person{
  constructor(public age: number, public lastName:string){
  }
}

const joahan = new Person(20, "Jimenez")

